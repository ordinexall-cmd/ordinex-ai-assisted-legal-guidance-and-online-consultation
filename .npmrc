# Always install frontend build tools (vite/typescript/@types).
# Render sets NODE_ENV=production on the service, which would otherwise
# make `npm install` skip devDependencies and break `tsc` / `vite build`.
production=false
